{
    "name": "fairbundled",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "postinstall": "cd frontend && npm install && npm run build && mv build ../backend/public && cd ../backend && npm install",
        "serve": "cd backend && npm run serve"
    },
    "author": "",
    "license": "MIT",
    "devDependencies": {
    },
    "dependencies": {
    }
}
